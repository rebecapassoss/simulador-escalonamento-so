version: '3.8'

services:

  simulador:

    build: .

    container_name: simulador_so

    volumes:
      - ./src:/app/src
      - ./inputs:/app/inputs
      - ./outputs:/app/outputs

    command: >
      --alg EDF
      --input inputs/caso2.json